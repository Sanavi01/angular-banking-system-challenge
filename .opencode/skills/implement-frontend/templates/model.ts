// Angular Model / Interface Template
// Ruta: src/app/core/models/<name>.model.ts

export interface {{ModelName}} {
  id: string;
  name: string;
  description: string;
  logo: string;
  date_release: string; // ISO date YYYY-MM-DD
  date_revision: string; // ISO date YYYY-MM-DD
}
